# Machine-specific overrides. Every *.conf file in this folder is loaded at the
# end of hyprland.conf, and all of them except this one are ignored by git.
#
# Example (local.d/monitor.conf):
#   monitor = , preferred, auto, 1.5
